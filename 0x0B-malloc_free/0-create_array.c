#include <stdio.h>
#include <stdlib>
#include "main.h"

/**
 * *create_array - creates an array of chars and intialize with a specific character.
 *
 * Return: NULL
 */
char *create_array(unsigned int size, char c)
{
	char myArray[];
	myArray = n;
	 if ( int size = 0)
		  return (NULL);

	 else
		 myArray() || NULL;
	
	 for (i = 0; i < size; i++)
	{
		cr[i] = c;
	}

	return (cr);
}
