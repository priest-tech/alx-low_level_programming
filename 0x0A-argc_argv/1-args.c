#include "main.h"
#include <stdio.h>
/**
 * main - prints the number of arguments passed to it.
 * @argc: counts number of arguments passed to a program.
 * @argv: an arrays of pointers to the arguments
 *
 * Return: Always 0.
 */
int main( int argc, char *argv[])
{
	printf("%d/n," argc -1);

		return (0);
}
